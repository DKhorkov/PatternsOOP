from .abstract_factory import AbstractFactory

"""
A factory method is a generative design pattern used to create specific implementations of a common interface.

It allows to separate the process of creating an object from the code that depends on the object's interface.
"""
